<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/webflow
                          http://www.springframework.org/schema/webflow/spring-webflow.xsd">


	<view-state id="userEdit"
		view="user/userEdit" model="personalInfo">
		<transition on="billing" to="validatePersonal" />
		<on-exit>
			<evaluate
				expression="registerHandler.addPersonalInfo(flowScope.registerModel, personalInfo)"></evaluate>
		</on-exit>
	</view-state>
	<action-state id="validatePersonal">
		<evaluate
			expression="registerHandler.validatePersonalInfo(personalInfo, messageContext)" />
		<transition on="success" to="billing" />
		<transition on="failure" to="personal" />
	</action-state>

</flow>